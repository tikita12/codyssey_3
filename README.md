# (여기에: 프로젝트 제목 — 예: 국내 여행지 추천 프로그램)

## 1. 프로그램 개요
<!-- 여기에: 날짜를 입력하면 LLM이 여행지를 추천하고, 지도 API로 맛집을 찾아 여행 리포트를 만들어주는 프로그램이라고 2~3줄로 소개 -->

## 2. 주요 기능 / 동작 흐름
<!-- 여기에: ① LLM으로 1차 추천 → ② 지도 API로 맛집 검색 → ③ LLM으로 최종 리포트 생성, 이 3단계 흐름을 순서대로 작성 -->

## 3. 개발 환경 / 요구 사항
<!-- 여기에: Python 3.10 이상, 터미널 실행 필요, 사용한 라이브러리(requests, python-dotenv 등) 목록 작성 -->

## 4. 설치 방법
<!-- 여기에: 코드 내려받는 법 + 라이브러리 설치 명령어(예: pip install -r requirements.txt) 작성 -->

```bash
# 예시
git clone (저장소 주소)
cd (폴더명)
pip install -r requirements.txt
```

## 5. API 키 설정 방법 (필수)

### 5-1. 사용한 API
<!-- 여기에: LLM은 OpenAI/Gemini 중 무엇을, 지도는 Kakao/Naver 중 무엇을 골랐는지 작성 -->

### 5-2. 환경변수 / .env 설정 방법
<!-- 여기에: .env 파일에 넣을 키 이름 안내. 실제 키 값은 절대 쓰지 말고 자리표시자만! -->

```bash
# .env 파일 예시 (실제 키 값은 넣지 마세요)
OPENAI_API_KEY="YOUR_KEY"
KAKAO_API_KEY="YOUR_KEY"
```

```bash
# macOS / Linux (현재 세션에만 적용)
export OPENAI_API_KEY="YOUR_KEY"

# Windows PowerShell (현재 세션에만 적용)
$env:OPENAI_API_KEY="YOUR_KEY"
```

### 5-3. API 키 발급처
<!-- 여기에: LLM 키·지도 키를 발급받는 사이트나 방법을 간단히 안내 -->

## 6. 실행 방법
<!-- 여기에: 실제 실행 명령어. 필수 옵션 -date 사용법 작성 -->

```bash
python travel_planner.py --date "2025-03-15"
```

## 7. 입력값 규칙
<!-- 여기에: 날짜는 "YYYY-MM-DD" 형식, 형식이 틀리면 사용법을 출력하고 종료된다는 점 작성 -->

## 8. 실행 결과물 확인 방법
<!-- 여기에: results/ 폴더에 ① 원본 데이터 JSON ② 최종 리포트 .md 파일이 생성됨. 파일 이름 규칙도 안내 -->

## 9. 결과 파일 구조 설명

### 9-1. 원본 JSON에 담기는 내용
<!-- 여기에: 1차 추천 JSON(recommended_city, weather, events, reason) + 맛집 목록(0건 가능) + 오류 요약(errors) 이 들어간다고 작성 -->

### 9-2. 최종 리포트(Markdown) 항목
<!-- 여기에: 추천 지역 / 추천 이유 / 날씨 요약 / 행사·축제 / 맛집 추천 / 1일 일정 제안(오전·오후·저녁) / 오류 요약 순서로 들어간다고 작성 -->

## 10. 에러 처리 정책
<!-- 여기에: ① 키 없으면 즉시 종료+안내 ② 지도 API 실패 시 맛집 "데이터 없음" 처리 후 리포트는 계속 생성 ③ LLM JSON 파싱 실패 시 딱 1회만 재시도 -->

## 11. 보안 주의 사항 (필수)
<!-- 여기에: API 키를 코드/README/결과물에 절대 직접 쓰지 않음, .env·환경변수로만 관리, 그 이유(실수 공개 방지 / 키 교체 편리 / 과금 사고 예방)까지 작성 -->

## 12. (선택) 보너스 기능
<!-- 여기에: 복수 지역 추천이나 결과 캐싱을 구현했다면 동작 방식 설명. 안 했으면 이 항목 삭제 가능 -->
